# Git Configuration
# Edit with your personal information

[user]
	name = Richard Kiene
	email = richard@liquescent.dev
	signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHyTKmfAwcJcdfKXmj2h3mwfgPaelE6gSMrquAcPmW09

[commit]
	gpgsign = true

[gpg]
	format = ssh

[gpg "ssh"]
	allowedSignersFile = ~/.config/git/allowed_signers

[core]
	editor = nvim
	autocrlf = input
	excludesfile = ~/.gitignore_global
	pager = delta

[init]
	defaultBranch = main

[pull]
	rebase = false

[push]
	default = simple
	autoSetupRemote = true

[alias]
	# Short aliases
	st = status
	co = checkout
	br = branch
	ci = commit
	
	# More complex aliases
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	last = log -1 HEAD
	unstage = reset HEAD --
	
	# Show branches sorted by last modified
	recent = branch --sort=-committerdate
	
	# Amend last commit
	amend = commit --amend --no-edit

[color]
	ui = auto

[diff]
	tool = nvimdiff
	colorMoved = default

[merge]
	tool = nvimdiff
	conflictstyle = diff3

[credential]
	helper = osxkeychain

# GitHub CLI integration
[credential "https://github.com"]
	helper = 
	helper = !/opt/homebrew/bin/gh auth git-credential

[credential "https://gist.github.com"]
	helper =
	helper = !/opt/homebrew/bin/gh auth git-credential

# Delta (better diffs)
[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	light = false
	side-by-side = true
